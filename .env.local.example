# Vercel KV — get these from your Vercel dashboard after adding KV storage
KV_URL=
KV_REST_API_URL=
KV_REST_API_TOKEN=
KV_REST_API_READ_ONLY_TOKEN=

# Optional: protect the /api/generate endpoint
# Set this in Vercel env vars and pass ?secret=YOUR_SECRET to trigger manually
CRON_SECRET=
